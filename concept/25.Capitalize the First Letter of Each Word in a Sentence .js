const word = "hi goodMorning shivam";

const capitalizeFirstLetter = (word) => {
  const capitalizeSentence = "";

  const ArrOfStr = word.split(" ");

  console.log();
};

capitalizeFirstLetter(word);
